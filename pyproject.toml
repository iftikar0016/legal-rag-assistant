[project]
name = "legal-rag-assistant"
version = "0.1.0"
description = "Legal RAG Assistant using LangChain and AutoGen"
requires-python = ">=3.9"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
